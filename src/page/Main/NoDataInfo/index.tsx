import style from './style.module.scss';

const NoDataInfo = () => (
    <div className={style.wrapper}>
        <p>So empty... Type something in search bar :)</p>
    </div>
);

export default NoDataInfo;
